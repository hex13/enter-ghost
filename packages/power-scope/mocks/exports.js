export const something = ':)';

export function foo () {
};

//------

// pseudo import, temporary structure
// Power Scope doesn't support multi file indexing yet
import__foo;

import {foo} from 'whatever';


export default Tutaj = 2;
